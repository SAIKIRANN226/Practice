- name: installing nginx
  hosts: localhost
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.yum:
      name: nginx
      state: present
  - name: run nginx
    ansible.buuiltin.service:
      name: nginx
      state: started