- name: Loops 
  hosts: localhost
  become: yes 
  tasks:
  - name: Installing packages
    ansible.builtin.package:
      name: "{{item.name}}"
      state: "{{item.state}}"
    loop:
    - {name: 'nginx', state: 'present'}
    - {name: 'mysql', state: 'present'}